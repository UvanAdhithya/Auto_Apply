selectors:
  login_btn_check: "button#login-button"
  login_modal_visible: "div#login-container"
  username_field: "input[name='email']"
  password_field: "input[name='password']"
  login_submit: "button[type='submit']"
  login_modal_hidden: "div#login-container"
  internship_listings: "div.individual_internship"
  company_name: "p.company-name, span.company-name, div.company_name p"
  role_name: "h2.job-internship-name > a.job-title-href"
  listing_url: "h2.job-internship-name > a.job-title-href"
  apply_now_btn: "button:has-text('Apply now'), a:has-text('Apply now')"
  proceed_btn: "button:has-text('Proceed'), a:has-text('Proceed')"
  submit_btn: "button#submit, input#submit, button.submit_button_to_show, button:has-text('Submit'), input[type='submit']"
  preferences_checkbox: "input#matching_preference"
  search_input: "input#keywords"
  search_btn: "button#search"
  overlay_close: ".close_action, i.ic-24-cross, .close, span:has-text('×')"